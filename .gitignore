node_modules/
.expo/
dist/
web-build/
*.log
.env
.env.local
.DS_Store

# Supabase CLI binary + temp state
sb.exe
supabase/.temp/

# Python cache
__pycache__/
*.pyc

# Notification + scraper logs
scripts/notify.log
scripts/scrape.log

# Scraped event data (regenerated daily)
data/scraped_events.json
data/venue_schedules.json

# Weekly live-music cache (regenerated every Monday) + its rolling backups
data/weekly_music.json
data/weekly_music_backups/

# Local scheduled-task XML backups (machine-specific)
scripts/task_backups_*/

# Venue registry — seeded from scripts/seed_venue_descriptions.py,
# accumulates discovered subpages + per-URL success stats at runtime
data/venue_urls.json
data/venue_urls.json.bak

# LLM-suggested new-venue candidates pending manual review
data/venue_candidates.json

# City-discovery output (per-city validated source lists)
data/city_sources/

# Claude Code local settings
.claude/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli